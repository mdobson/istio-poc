Setting up some k8s networking samples.

This repo contains sample configs for securing workloads using a GCP distributed token. It also includes addtional authorization rules for further securing a workload based on token claims.
